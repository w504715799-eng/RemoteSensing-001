# Spain 冻结记录

2026-09-08执行补充：最终协议已按用户后续授权完成48 ROI／336预测及独立进程缓存
核验，科学代码未改。真实包受限重建和全成员映射均通过，不再是待验证状态；源nodata
语义未知仍然保留。见[结果报告](first-paper-spain-results.md)。以下为冻结时的历史记录。

## 当前正式版本：费用由用户管理

2026-09-08 用户明确“不需要关心我的费用，专注于功能开发”，覆盖下方历史计费等待项。
已实现并审阅本地 `freeze` 命令，生成
[正式协议](../../paper/protocols/spain-external-frozen-v1.json)，SHA-256：
`798200c5c8b2cd9102755202810e8161832064de686a81b41c02776ddaf85525`。
127个实现文件映射的canonical SHA为
`e4be25b3f1182dbf795c9397d89113a4047d9a2a91723796a14e906e56c4cc4c`。
草案同步更新，SHA为 `ac5ce4302fc1766d162967403961ef7a4366be27f15b04fdf76e080d0cbbb594`。

正式文件中 `status=frozen`，`budget.cost_management=user`；无需小时价／货币字段，
保留2100秒规划和2700秒单次超时，计时证据仍绑定下方预算草案的真实SHA。
五评分、两子集全成员、模型seed／身份、输入限制、失败规则和运行版本均未改变。
`freeze` 只生成并自校验文本，禁止覆盖冲突文件，不调用运行环境预检或外部数据入口。
新增7项回归先失败后通过；协议／CLI共29项通过（1.96秒），执行／发布5项通过
（12.40秒），变更文件Ruff通过。只读代码审阅无阻塞问题；未跑全量测试或访问真实Spain。

后续聚焦实际功能集成和外部运行结果，不再索取费用信息。来源兼容性仍待首次真实
受控加载验证，最终协议存在不等于已经获得外部结果。以下为冻结前的历史审阅依据。

## 历史候选检查点（由上述正式版本取代）

2026-09-08，状态：**候选审阅完成，费用待补；未最终冻结**。
本记录整理 Tasks 1–5 已交付内容供 Task 6 定稿，不是外部数据访问或 GPU 执行凭证。
本轮只读取 Git 中的小型文本证据及代码，未连接云端或打开任何影像、模型和预测缓存。

## 候选身份

受审实现提交：`ff9c75d4262abbd0bd24e0beb0dc4217b9df665d`。
[候选协议](../../paper/protocols/spain-external-draft-v1.json)仍为 `status=draft, budget=null`，
SHA-256：`dfe9239b162136a15af532810674a89fbc4e7252520bad2d90306e9f4ef7897c`。
其中127个Python文件的路径／哈希映射 canonical JSON 摘要为
`4151fda7ee22d6666dfe9a0c0c134a6c4d91eca05ddff72322740dad85aea8cb`。
本轮仅更新说明文件，不改变该实现或候选摘要。最终文件必须另行生成并独立审阅其SHA，
不能把此草案摘要当成正式运行摘要。

## 确切数据与计算量

固定来源提交 `e4600b9c74a621adeec047e5f6cc7a2d70a58134`，使用100版本全部成员。

| 子集 | 包路径 | 字节数 | ROI数 |
|---|---|---:|---:|
| Crops | `100/spain_crops/spain_crops.pkl` | 140385908 | 28 |
| Urban | `100/spain_urban/spain_urban.pkl` | 100276003 | 20 |

Crops 包SHA：`5f68a962b200ef781cbabbe21e598b71379fafb3c12d614deae652e65bb86319`。
Urban 包SHA：`da6d0ae68a78ef9c125d3b3805fde40718e61b1e54db2877d8041b049c2c3775`。
总计240661911字节；包内完整成员及六字段身份已在候选协议逐项列明。

每ROI七份预测：LDSR seeds 3407–3411、bicubic、SEN2SRLite；全矩阵最多336份，
其中LDSR 240次、bicubic 48次、SEN2SRLite 48次。模型仅接收LR，全部五评分复用
同一批预测。完整HR网格512×512，不选择更有利的子窗口。真实运行只补未尝试的缺失槽。
执行后两轮缓存科学复算，不新增模型推理；完整成功缓存无模型构造。

## 科学规则与输入限制

主比较为每ROI `AURC_R9(K5)-AURC_R9(LR)`，对子集有效配对等权平均，负值有利于K5。
Crops与Urban分别报告，描述性统计，不做显著性检验、bootstrap或独立ROI置信区间。
五评分为LR、三模型、K5、窗口3邻域分歧、随机解析期望；次要指标保留R1、rho、
覆盖率0.1到1.0步长0.1的十点曲线。原K5阈值 `7.970395366024563e-06` 仅用于迁移描述。
两子集展示均固定 `ROI_00001`、`ROI_00002`，失败保留空格，不替换样本。

研究数值处理明确为选定RGBN、保留零、拒绝非有限或选择波段范围外值、超过10000
饱和并计数、除以10000；源nodata未知项不会因此消失。真实包内成员及受限pickle
重建兼容性仅能在正式访问时核验，失败即停止，不普通pickle回退、不扩大allowlist。
模型预测失败保留逐方法分母；主比较、迁移和结构诊断使用各自有效分母，不静默删ROI。
结构诊断保留隔离OpenSR 1.3.3 softmin图像均值，配准失败／无共同有限支持为缺失，
不解释为硬幻觉像素比例或逐像素检测证据。官方质量字段曾有限暴露，不用于样本选择。

## 环境、核验及旧阶段隔离

运行版本由 [原云端CPU证据](../../paper/tables/cloud-cpu-execution-v1.json) 固定；
CPU为原AMD EPYC 7K62，LDSR为原RTX 4090配置。SEN2SRLite使用96线程兼容策略，
其他评分串行单线程。不得以更换环境后的结果沿用本测量或在Spain上重新选配置。
此前已通过云端25项定向测试、本地协议／CLI22项和数据政策7项检查；本轮文档更新
只核验文本证据摘要、实现映射和引用，不重跑这些测试或全量套件。

旧C小型验收文件SHA为 `6fda78e13b13f78201487043fe9f679b432b692216778d172e7f36bf2d9ded21`，
`phase_decision=empirically_met_but_inconclusive`，维持终局。此次只核对已发布文本，
没有重开原始数据／缓存。正式输出将使用协议绑定的新标识，目录须在仓库、包目录、
模型目录及历史阶段之外；确切云端路径在部署时验证，不能把候选目录规则写成已部署事实。
发布前要求两轮科学JSON一致，manifest最后落盘；具体命令和恢复语义见
[执行手册](../first-paper-spain-execution-runbook.md)。

## 预算与剩余定稿项

[预算草案](../../paper/protocols/spain-budget-draft-v1.json) SHA为
`a77e313b6932c6cbc69778b4efaca38b9786b2d654423d1ff6911683d96f598c`。
名义推理／两轮CPU复算／写缓存代理1122.132718秒，约18.70分钟。规划总时长2100秒
（35分钟），含600秒输入获取／加载假设及377.867282秒其他余量。建议run单次上限
2700秒（45分钟）；不包含所有部署、下载、空闲计费，也不是整项费用上界。

仍缺实际小时价、计费粒度以及由此得出的费用预算；不得填猜测值或沿用占位证据SHA。
本轮已向用户询问价格。补齐后完成费用审阅、另存最终协议、核对实现与预算证据摘要，
再依据会话授权范围判断正式执行所需条件。Tasks 5成本、6最终冻结及7外部执行仍未完成。
