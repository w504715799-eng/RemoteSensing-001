# 邻域适配 development 比较

2026-09-07。两轮全量 CPU 计算完成，科学结果字节完全一致，已持久化并发布数值证据。
实现提交：`defa6fa4857d17d173915c80ebe8fdf780732f8f`。
访问范围：固定 development 120 ROI、K5 seeds 3407–3411，全部复用既有预测，
新增 GPU／模型推理次数为 0。没有下载原始数据，也没有访问 B/C 像素或缓存。

## 输入核验与环境

固定清单、A 结果、A 缓存审计 SHA-256 均校验通过；在计算候选前完成 240 个 LR/HR
资产及 600 个 K5 预测的文件身份核验。每个 ROI 的归一化 LR/HR 和 R1/R9 图与
已发布张量哈希一致，原 K5 AURC 的复算绝对误差不超过 1e-12。
不使用其余历史冒烟预测。输出在独立目录，未覆盖旧 artifacts 或缓存。

使用云端已有 base Python 3.12，PyTorch 2.12.1+cu130，单线程 CPU，GPU 不可见。
用户明确授权后安装 pytest 8.4.2、iniconfig 2.3.0；未新建环境或改变模型依赖。
本地相关测试 78 passed，云端新增评分／入口测试 28 passed。
此前因用户要求确认解释器而停止过一次运行，无最终结果，没有因此改候选或筛样。

## 已复算结果

| 邻域候选 | R1 AURC | R1 rho | R9 AURC | R9 rho |
|---|---:|---:|---:|---:|
| 3×3，sigma=1 | 0.008826283496358979 | 0.4607505585497245 | 0.009132920194314142 | 0.7387422780761966 |
| 9×9，sigma=1 | 0.009075755651976030 | 0.4214213534624263 | 0.009214892376785777 | 0.7105703767432385 |

按访问前规则选中 **3×3、sigma=1、Gaussian radius=3、reflect padding**。
没有新增搜索或改变原 K5 主比较。相同 120 ROI 上，其 R9 AURC 比原 K5 低约
4.60%、比 LR 残差低约 9.83%、比三模型分歧低约 5.53%。这些是参与选参的开发集
描述性差异，不是显著性检验、独立确认或跨域保证，也不表明幻觉检测已经验证。
两个候选均保留，外部主比较仍为 K5 对 LR，邻域适配作为预设比较完整报告。

## 首轮成本

完整首轮含文件核验、加载、基线复现、两候选 R1/R9 诊断及评分耗时 264.0334 秒。
单 ROI 纯评分计时均值：3×3 为 0.269405 秒，9×9 为 0.352836 秒；中位数分别为
0.268328、0.349094 秒。每个候选 120 次，均为 CPU 计时，不含原 K5 推理成本。
这不是 GPU 基准，不能将 4.4 分钟用作 Spain 240 次 LDSR 的费用估计。

## 核验状态

首轮科学 JSON SHA-256：`59e006880224542061ca7b3549a5ba9bc5d873aed1a7bb23603aa2edc5dc2a50`。
第二轮使用同一代码、相同输入、相同候选与次序，耗时 265.3517 秒；科学 JSON
字节完全一致。持久化副本经同步、重新读取比较及 SHA-256 检查通过。
本地仅接收 [数值证据](../../paper/tables/neighborhood-development-v1.json)，独立核对
120 ROI 成员、480 条诊断记录、四组均值与十点曲线平均值，并核对 canonical JSON。
没有把相同程序的重复运行冒称为独立算法复现。

运行记录 SHA-256（文件仅保存在云端持久化存储，不进入 Git）：

- 首轮：`0e1d0867918a9eb9f464394667520e379d75e27270546dc5af8cb4ddc6aad3be`。
- 复算：`2b8f8a7d5cbfcfe27247e4e617a7b511ddbcd0cda0238b1f3d5e7fba4d7c73ae`。

最终检查无本任务计算进程、无 GPU 计算进程，已通知用户可关机并保留持久化数据盘。
原始像素、缓存和运行环境仍留在云端。Spain 协议及新推理成本估算仍未完成，
本任务的完成不等于第一篇论文研究全部完成。
