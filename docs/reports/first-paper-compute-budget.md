# 第一篇论文计算预算（未实测）

2026-09-07。本地 CPU 已实现邻域评分和已发布证据渲染，不需要 GPU。
没有启动云服务器，没有新真实图像推理，没有读取 development／B／C 原始缓存。
本地工作区没有 `data/` 目录，开发缓存是否仍在独立可用存储中尚未知。

## 下一次资源请求的范围

先确认允许的 development 120 ROI、K5 seeds 3407–3411 对应资产和缓存位置。
只做 development 身份核验，不能广泛枚举包含 B/C 的服务器目录。
若缓存完整，新邻域分数可在 CPU 上计算，不必为它租 GPU。
若缺失，先列出确切缺失项与计算量，再请求 GPU；不自动重新运行旧阶段。

## 外部预算公式

名义 48 ROI：240 次 LDSR + 48 次 SEN2SRLite + bicubic／CPU 后处理；实际成员
须待数据合同确认。邻域与 K5 共享预测，不重复计 240 次。
默认单 worker；不开展新的并行推理优化。

`GPU hours = (240*t_LDSR + 48*t_SEN2SRLite)/3600 + measured overhead_hours`。

需记录模型加载、预处理、推理、写盘、CPU 评分时间、峰值显存、缓存命中率和切块次数。
供应商、小时价、实例配置、时间上限尚未指定，因此当前不能给出可信费用或 GPU 时长。
SPOT／允许的 development 冒烟测量在最终外部冻结前完成，不拿 Spain 测速或调试。
