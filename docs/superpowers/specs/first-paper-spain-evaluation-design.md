# Spain 外部评估协议

状态：FROZEN，2026-09-08；最终机器协议及其SHA见
[冻结记录](../../reports/first-paper-freeze-record.md)。用户明确费用自行管理，价格不再是前置条件。依据
[元数据审计](../../reports/first-paper-spain-metadata-audit.md)。
development选择、入口测试与来源限制审阅已完成；真实包重建兼容性待正式加载验证。

## 数据合同与未决条件

固定 HF 提交不变，候选包改为 **v2／100**，与公开 CSV 同版本；具体路径、SHA 见审计补充。
此决定发生在任何 Spain 图像访问前，未改变主比较、样本范围或根据模型效果筛选数据。
版本化数据卡已确认 L2A 的 RGBN 索引 `[3,2,1,7]`、HR RGBN 顺序、×4、512×512
HR，以及 `HRharm` 相对 L2A 的协调关系。输出意向仍为 [0,1] 反射率。
不得直接把 SPOT 的除以 10000、clamp、HR 字段名当成 Spain 已确认合同。
官方用例展示除以 10000，但 Spain 包特定的 dtype／nodata／异常范围约定仍未完整核实。
不再要求解释 100→021 内容差异；最终入口必须核验同版本包的嵌入 metadata 与固定清单，
通过后才能按 metadata 行定位图像；不允许假定字典序就是张量顺序。
2026-09-08 冻结前审阅：独立受限解包、成员绑定和异常处理已经实现并通过合成验证，
原 SPOT-only loader 保持不变。选定的数值合同要求 LR 为 (12,128,128)、HRharm 为
(4,512,512)，选择 LR RGBN 后检查有限值及 [0,32767] 范围；零保留，超过10000
逐波段计数并饱和，再除以10000输出float32。该研究处理规则不等于源 nodata 语义已确认。
固定包大小／SHA 校验先于受限子进程加载；嵌入六字段身份逐项核对后按实际行定位。
真实包重建兼容性仍未验证，未知类型或合同错误应停止，不自动扩展解包许可或换包。
详见 [冻结前审阅记录](../../reports/first-paper-freeze-record.md)。

## 比较与统计

2026-09-08 执行身份补充：SEN2SRLite 使用独立 `CloudSEN2SRLiteX4` CPU 96 intra-op
线程策略，串行单 worker，加载／预测后恢复调用者线程数。provenance 必须进入新缓存键，
不混用历史未记录线程策略的缓存。已在三个固定 development ROI 复现旧哈希；
更换硬件／依赖／backend 必须重核验，不以 Spain 调参。详见
[CPU 核查](../../reports/first-paper-sen2sr-reproducibility.md)。其他冻结模型配置不变；
此补充不解除其余外部访问门槛。

- 中心 LDSR seed 3407；K5 seeds 3407–3411；模型身份沿用冻结记录。
- Crops、Urban 分开。主比较是每 ROI 的 `AURC_R9(K5)-AURC_R9(LR)`，
  再对子集有效配对 ROI 等权平均。负值有利于 K5；不跨子集合并像素。
- 五评分：LR、三模型分歧、K5、development 选定的邻域适配、随机解析期望。
  R1、rho、固定十点覆盖曲线与部署成本为次要结果，完整保留不利列。
- 当前固定**描述性分支**：报告均值、配对差分布、ROI 数、HR 来源计数、失败分母；
  不报告显著性检验或独立 ROI 置信区间。HR 来源仅 5／4 组且共享，暂不启用 bootstrap。
  因不做检验，无多重比较校正；不得把此解释为多个确认性发现。
- 原阈值 `7.970395366024563e-06` 只应用于 K5 作迁移描述。报告覆盖与 ROI 最大
  R9 损失及全拒绝数，不产生新的原 C 决策。其他分数只按相同覆盖预算比较。

## 缺失、停止与展示

成员在像素访问前冻结。只允许按身份、数据合同、文件完整性和非有限值规则拒收；
不按误差或质量分数过滤。主比较按完整配对，逐方法报告有效／计划分母及原因。
若无法形成任何有效配对，该子集标为无法评估，不补入有利样本。
身份或数据合同不一致时停止整批，不自动换版本、改归一化或删除指标。
只补从未尝试的同身份缺失预测；失败／中断槽不自动重试。中断留下的有效完整缓存
可核验后恢复，已成功缓存丢失或损坏应停止；不依据中途结果扩样或换方法。
展示每子集按冻结 ROI ID 字典序最前两项；失败则保留失败格，不用其他 ROI 替代。

结构诊断目前限定为图像级探索性结果：默认 softmin 分量均值，不命名为硬幻觉像素比例。
16 像素裁边及 SR 配准后的地图不与原始评分逐像素对应。不新增像素级幻觉检测主张。
2026-09-08：有效值／配准失败规则与合成验证已完成，候选协议保留该探索性诊断。
隔离 OpenSR 1.3.3 子进程显式检查实际配准失败，三分量使用共同有限支持；无效值为
null 并保留失败原因，不填零，不改用另一配准器。原云端合成两轮复算逐字节一致。
元数据中官方质量字段的有限暴露必须在最终协议及论文复现说明中披露。

最终协议绑定可执行实现及完整运行版本。费用由用户管理，不再询价或等待费用审批。
已完成的测量和诊断不重复执行，外部结果仍须实际运行后才能报告。
