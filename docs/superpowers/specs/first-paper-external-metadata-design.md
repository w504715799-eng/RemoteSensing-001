# 外部元数据投影与指标合同检查

2026-09-07。只处理公开文本和合成张量，不下载 Spain 像素，不连接已可关机的云端。

元数据工具 `scripts/paper/audit_spain_metadata.py`：固定 HF 提交与已审计的两份 100
目录 CSV 的 SHA-256，校验字节后仅投影 roi、lr_file、hr_file、lr_gee_id、crs、affine。
不处理质量列，不以质量列筛样；成员按 ROI ID 排序。输出
`artifacts/datasets/spain-metadata-v1.json`。外部候选改为同版本 100 数据包，
显式标记图像内容与成员对应仍须受控加载核验，不从目录同版本推断逐项核验已完成。
验证计数、唯一 ID、非空来源、CRS 和六个有限 affine 数值；不推断来源组独立性。

TDD 步骤：先写 SHA 变化拒绝、重复 ROI 拒绝、质量字段不可输出、来源计数与排序测试；
实现最小投影及只读网络入口；生成后复核两子集行数、哈希与全部输出字段。

OpenSR 检查只使用已安装 1.3.3 与合成张量。必须明确：默认 16 像素裁边、四倍尺度
下 LR 裁 4 像素；ha/om/im 默认是 softmin 分量均值，不是硬分类像素比例；
无有效像素不能填 0。先写必要的上游合同特征测试，不修改冻结的旧指标适配器。
后续如保留结构诊断，先限定图像级，禁止将已配准地图直接与未配准分数逐像素相关。
