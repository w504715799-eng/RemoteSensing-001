# Computational Cost, Error Scale, and Threshold Transfer of Uncertainty Scores for Sentinel-2 Super-Resolution

研究初稿，2026-09-08。预设实验与本轮方法、文献和论证审阅已完成；尚未按目标期刊编排。

## Abstract（工作稿）

本研究围绕 Sentinel-2 超分辨率中“评分能否支持可信区域选择”展开，依次考察
评分的成本与排序表现、空间邻域适配，以及固定阈值跨地域使用的边界。
沿用development选择与已消费的内部校准／测试，另在冻结的Spain Crops 28 ROI及
Urban 20 ROI上比较五评分。K5相对LR的平均R9 AURC分别降低6.16%和8.64%，
开发选定的3×3邻域适配在两个子集的R1/R9平均AURC最低；K5在Crops R1 AURC及
两子集R1 rho上并不优于三模型分歧。原K5阈值迁移后的平均覆盖分别为47.43%和
23.51%。完整336份预测与两轮缓存科学复算用时20分34秒，无预测失败。
这些是相关ROI上的描述性结果，不构成普遍风险保证；内部终局仍然inconclusive。

## 1. Introduction

Sentinel-2 超分辨率增加空间细节的同时，也需要说明哪些重建区域更可信。
不确定性分数的排序能力、保留区域的平均误差和对 ROI 最坏局部误差的风险控制
是不同问题。更昂贵的随机采样即使改善排序，也未必提供足够强的有限样本证据。

本研究固定 RGBN、L2A、×4 和中心重建，比较评分成本与误差尺度的关系，继而
描述一个固定校准阈值在外部地理数据上的表现。不训练新网络，不将更低像素误差
等同于幻觉检测或下游任务收益。背景与来源见 [相关工作](related-work.md)。

研究问题按“评分评价—空间适配—使用边界”组织：

1. **评价评分：** 同一中心重建下，增加随机样本相对低成本一致性代理带来多大
   排序增益，对应多少额外调用；这种关系是否随 R1／R9 误差尺度而变化？
2. **适配空间结构：** 在复用相同 K5 输出的条件下，加入联合种子／邻域二阶矩
   能否降低选择风险，且开发选出的配置能否在冻结外部样本上保持优势？
3. **检验使用边界：** 当相对排序进一步变成固定阈值的保留规则时，原 K5 阈值
   跨地域产生什么覆盖与 ROI 最大保留损失，现有证据支持多强的风险声明？

三者是研究问题的递进，不是三个算法模块的串联。第一步提供 K5 参照；第二步
研究这一参照的空间评分适配；第三步独立检验原 K5 的阈值迁移。邻域分数没有在
本研究重新校准，不能将其较低 AURC 写成已证明更好的阈值迁移或风险控制。

相应贡献是：固定中心和成本口径下的尺度相关比较；无需新增模型推理的邻域
公式 RGBN 适配及外部验证；区分排序表现、覆盖和风险证据的固定阈值迁移分析。
本文把这些证据组织成一个评价过程，不提出新的共形定理或未经验证的端到端保证。

## 2. Data and study roles

现有 SEN2NAIP v2 development、calibration、internal_test 各 120 ROI，分别承担
选择、拟合与一次性终局评估。开发结果不能当作独立确认。协调后的 HR 是参考，仍有
时间、配准和跨传感器差异。内部测试已经消费，不重新读取像素或重新选择模型。
Spain 外部评估使用固定 HF 提交下的 v2／100 包与同版本 CSV，Crops 28 ROI、
Urban 20 ROI全部执行。包哈希、受限解包及内嵌六字段成员／行位置绑定均通过，
无需改变解码规则。两个子集分别报告，不把共享HR来源或相邻ROI当成独立重复。
版本、来源和独立性限制见元数据审计与[外部报告](../docs/reports/first-paper-spain-results.md)。

## 3. Methods

### 3.1 Fixed reconstruction

中心预测固定 LDSR seed 3407；K5 使用 3407–3411。评分比较包括 LR 重投影残差、
三模型分歧、K5 方差、邻域二阶矩适配和随机解析基线。随机解析基线不额外推理，
邻域适配复用 K5。报告独立部署调用数和整个研究实际调用数，避免重复计算缓存成本。

中心不是五样本均值。采用 opensr-model 1.1.1 的 `opensr-ldsrs2_v1_0_0.ckpt`，
100 sampling steps、eta=0.95、temperature=1，启用 histogram matching，预测
截断到 [0,1]。SEN2SRLite 使用 sen2sr 0.8.5 和独立 CPU 96 线程适配器。
权重 SHA、模型配置和完整依赖绑定在[冻结协议](protocols/spain-external-frozen-v1.json)，
实际身份见[执行证据](tables/spain-execution-v1.json)。本研究不训练模型。

### 3.2 Score definitions and calls

记 LR 为 $X$、协调参考为 $Y$、五个 LDSR 输出为 $S_k$，中心为 $S_0$。
$D_4$ 是 area 四倍下采样，$U_4$ 是重复为 4×4 块，$b$ 为四个波段；
$B_w$ 是反射边界的 $w\times w$ 均值池化。分数不读取 HR：

$$
s_{LR}=U_4\left[\frac14\sum_b|D_4(S_{0,b})-X_b|\right],\qquad
s_{K5}=\frac14\sum_b\left[\frac15\sum_{k=0}^4 S_{k,b}^2-
\left(\frac15\sum_{k=0}^4 S_{k,b}\right)^2\right].
$$

三模型分歧用中心、bicubic 和 SEN2SRLite 输出代入总体方差定义，分母为 3。
LR 残差是固定面积降采样的一致性代理，不是已标定的传感器成像模型。邻域适配为

$$
s_{N,w}=G\left\{\frac14\sum_b\left[
B_w\left(\frac15\sum_k S_{k,b}^2\right)-
\left(B_w\left(\frac15\sum_k S_{k,b}\right)\right)^2
\right]\right\}.
$$

$G$ 是 sigma=1、半径 3 的归一化 7×7 Gaussian 卷积，使用反射边界。
邻域计算为 CPU float64，舍入产生的微小负方差截零。它包含联合种子／空间变化：
即使五样本相同，空间纹理仍可产生正值，不能称为仅平滑 K5 或纯随机不确定性。
development 比较 w=3、9，按平均 R9 AURC 选较低者，差不超过 $10^{-12}$ 时
选较小窗口；选中 w=3 后外部固定。方法归属和差异见[相关工作](related-work.md)。

| 单独部署的评分 | 每 ROI 所需输出／调用 | 相对已有中心额外调用 |
|---|---|---:|
| LR／随机解析基线 | 1 次 LDSR | 0 |
| 三模型分歧 | 1 次 LDSR + 1 次 bicubic + 1 次 SEN2SRLite | 2 |
| K5 | 5 次 LDSR | 4 |
| 邻域 w3 | 同一 5 次 LDSR，加 CPU 后处理 | 4 |

bicubic 计为一次基线输出计算，不是神经网络前向。完整研究共享输出，每 ROI
共 7 槽，48 ROI 共 336 份；不能累加独立部署成本。随机基线不生成新的重建。

### 3.3 Error scale and ranking

$$
R_1(p)=\frac14\sum_b|S_{0,b}(p)-Y_b(p)|,\qquad R_9=B_9(R_1).
$$

R9 在完整网格计算后才应用保留掩码；先取绝对值再空间平均，窗口宽 22.5 m。
分数升序保留 $\lceil cN\rceil$ 个像素，同分按展平索引排序；选择风险是保留位置
的风险均值。十点离散 AURC 是 c=0.1,…,1.0 的选择风险算术均值，不是梯形积分。

主要排序指标为逐 ROI 的十点离散 R9 AURC；R1 与 Spearman 为次要诊断。
风险的反射边界、同分规则和 ROI 最大损失定义见 [指标字典](metric-dictionary.md)。
外部主比较为各子集内 K5 减 LR 的配对 AURC 差；负数表示 K5 更低。

外部输入选取LR RGBN索引[3,2,1,7]及HRharm RGBN，保留完整128→512网格。
选择波段须有限且在[0,32767]；零保留，超过10000的值饱和并计数，除以10000。
实际两子集饱和计数均为0，但这不确认源nodata语义。只使用描述性等ROI统计，
不做显著性检验、bootstrap或独立ROI置信区间；失败保留逐方法、配对和结构诊断分母。

归一化反射率本身无量纲；为区分尺度，R1、R9、AURC 和 LR 分数按反射率尺度
报告，三模型／K5／邻域方差及其阈值按反射率平方尺度报告。rho、覆盖和 softmin
分量无量纲；不同类型分数的绝对大小不能直接比较。

### 3.4 Frozen threshold and risk interpretation

阈值保留 $s_{K5}(p)\leq t$，ROI 损失 $L_i(t)$ 是保留位置最大 R9，全拒绝记 0。
校准在 n=120 个 ROI 上选满足下式的最大已观测分数阈值：

$$
\frac{\sum_{i=1}^{n}L_i(t)+1}{n+1}\leq\alpha,\qquad\alpha=0.05.
$$

没有可行候选时令 $t=-\infty$，全拒绝。归一化损失上限为 1，已发布阈值为
$7.970395366024563\times10^{-6}$。alpha 是期望 ROI 损失目标，区别于内部测试
上界的 confidence_error=0.05。共形期望风险结果依赖可交换性等条件，不等于
单侧高概率置信声明，也不自动覆盖地理迁移。
[Conformal Risk Control](https://research.google/pubs/conformal-risk-control/) 给出
这一期望风险框架。本研究保留原内部终局，在 Spain 只描述覆盖与损失，不重新校准。

### 3.5 Secondary structural diagnosis

OpenSR-Test 1.3.3 在独立 CPU 进程评估中心重建：裁边 16 得到 480×480 网格，
启用光谱与空间协调，PCC 配准距离上限 5、最大关键点设置 500；使用 ND 距离、
softmin 温度 0.25，im/om/ha score 参数均为 0.05，梯度掩码为 auto。
三分量仅在共同有限像素上平均，记录支持数；配准失败或无有效支持不填零。
此 HR 辅助协调仅服务结构诊断，不改变主评分或 R1/R9。softmin 分量不是硬分类
幻觉比例，裁边／配准后的结构网格也不能直接当作原评分网格。

## 4. Existing results

开发集 K5、三模型、LR 的平均 R9 AURC 分别为 0.0095736511、0.0096675805、
0.0101288090。K5 相对 LR 改善约 5.48%，但该结果参与方法选择。
在 R1 下 K5 的 Spearman 为 0.3959881512，低于三模型的 0.4040862072；
因此不能概括为所有尺度、所有指标上一致优越。

校准阈值为 7.970395366024563e-06，校准覆盖为 58.3230%。内部测试覆盖为
57.4102%，ROI 平均损失为 0.0419851，风险上界为 0.0779855，超过目标 0.05。
终局保持 **empirically_met_but_inconclusive**，流程验收不是风险确认。
所有数字来源见 [证据清单](../docs/reports/first-paper-evidence-inventory.md)。

已有数值表：[开发指标](tables/development_scores.csv)、[开发曲线](tables/development_curves.csv)、
[校准与终局](tables/calibration_evaluation.csv)。
曲线见 [R1／R9 图](figures/development_risk_coverage.pdf)，复建方法见
[复现说明](reproducibility.md)。

在同一 120 ROI 上新增的邻域适配比较中，3×3 与 9×9 的平均 R9 AURC 分别为
0.0091329202 和 0.0092148924。按预设规则选择 3×3，固定 sigma=1；两个候选均保留。
所选适配的 R9 AURC 比原 K5 低约 4.60%，但这是开发选择结果，不是独立确认。
其 R1 AURC 为 0.0088262835，R9／R1 rho 分别为 0.7387422781／0.4607505585。
所有评分复用已有 K5；新 GPU 推理为 0。两轮 CPU 计算产生完全相同的科学 JSON。
详见 [新增数值证据](tables/neighborhood-development-v1.json) 与
[执行报告](../docs/reports/first-paper-neighborhood-development.md)。

## 5. External results and compute

外部两子集336份预测全部完成，无预测或结构诊断失败。主要R9结果如下；各方法
Crops有效28/28、Urban有效20/20，越低越好。

| 评分 | Crops R9 AURC | Urban R9 AURC |
|---|---:|---:|
| LR | 0.0086521456 | 0.0133663676 |
| 三模型 | 0.0082680572 | 0.0125540375 |
| K5 | 0.0081193557 | 0.0122120479 |
| 邻域w3 | 0.0076976989 | 0.0116871587 |
| 随机解析期望 | 0.0103186074 | 0.0167936391 |

K5减LR的平均配对差分别为−0.0005327899和−0.0011543197，相对LR平均AURC
降低6.16%和8.64%；48个ROI配对差均为负，但空间相关限制独立性解释。
邻域w3的R1/R9平均AURC在两个子集均最低。Crops R1三模型为0.0077333810，
略低于K5的0.0077813464；两子集R1 rho也均是三模型高于K5。
完整次要结果及逐ROI差异保留在[评分表](tables/spain_scores.csv)与
[配对表](tables/spain_paired.csv)，不只报告有利指标。

固定K5阈值在Crops／Urban的平均覆盖为47.4338%／23.5099%，平均ROI最大保留
R9损失为0.0403563／0.0487801，两者全拒绝数均为0。覆盖的子集差异说明固定阈值
迁移需要单独描述；均值低于0.05不能替代有限样本风险确认。见
[逐ROI迁移表](tables/spain_transfer.csv)、[曲线](figures/spain_risk_coverage.pdf)和
[迁移图](figures/spain_threshold_transfer.pdf)。

OpenSR softmin图像级结构分量在全部48 ROI有效，但仅在裁后共同有限支持上计算：
Crops为76522–109368像素、Urban为80686–113173像素，裁后网格均230400像素。
不能将其解释为整图幻觉像素比例或像素级检测；逐ROI支持数见结构表。

正式进程1234.032秒（20分34秒），预测阶段485.764秒（含加载／缓存／状态写入），
两轮科学复算364.647／375.850秒；两轮科学JSON完全一致后发布。没有以这些墙钟
数字替代供应商费用或跨硬件吞吐保证。详见[完整报告](../docs/reports/first-paper-spain-results.md)。

开发阶段已测：单线程 CPU 每 ROI 纯评分均值，3×3 为 0.269405 秒，
9×9 为 0.352836 秒；不包含生成 K5 的成本。

随后在预定的 3 个 development ROI 上单独测量推理成本：RTX 4090 上 15 次 LDSR
常规调用平均 1.615943 秒／次（另计一次冷启动），SEN2SRLite CPU 为 0.209878 秒／次，
bicubic CPU 为 0.006185 秒／次。按相同配置外推 48 ROI 的模型调用约 6.637 分钟，
不是完整流水线时长或费用保证；详细边界见推理测速报告与数值表。该测量没有新质量指标。
LDSR 与 bicubic 输出哈希匹配历史记录。随后 CPU 诊断发现，SEN2SRLite 在 1 线程
复现测速输出，在云端 96 线程复现三个历史输出；最大像素差为 5.96e-7，三模型
评分的两个 R1 AURC 差超过本次严格诊断线，不能声称完全无影响。主比较 K5–LR
不依赖此模型。后续采用独立的 96 线程兼容适配器；三次验证调用含首次冷调用均值
0.888265 秒，更新推理部分投影为 7.179 分钟；正式流水线实测已在上文分别报告。
详见 SEN2SRLite 可复现性报告；这些仅是三个样本上的结果，不是跨平台保证。

## 6. Limitations and discussion

三个问题共同说明，评分排序、空间适配和阈值使用需要分别建立证据。K5–LR
比较支持本次外部 R9 排序增益，邻域结果支持复用输出改善平均选择风险；原 K5
阈值在两个子集产生不同覆盖，则说明绝对阈值的使用还需要单独评估。AURC 考察
多个覆盖水平的平均保留误差，阈值迁移另考察固定分数下的覆盖与 ROI 最大损失，
因此前者改善不能直接推出后者改善。本研究未做邻域阈值迁移，也未将其缺失的
证据用 K5 的迁移结果替代。

开发选择偏差、空间相关、预训练重叠未知、协调参考的误差以及有限 ROI 数量限制结论。
K5 五个种子不是五次独立复现，R9 改善也不表示 2.5 m 尺度细节均正确。
邻域分数的优势可能部分来自纹理复杂度与误差的相关性；现有比较不能分离空间
纹理与随机种子方差各自的贡献，不能据此断言后验不确定性估计更准确。
异构三模型与同模型五样本的调用数也不直接等价于计算量。墙钟测量绑定硬件、
线程和缓存边界；未评估下游决策收益，故不提出普遍成本最优结论。

本文贡献是固定中心下的可复现比较、尺度相关的正负结果，以及保留原终局的
地理阈值迁移描述。没有新增共形定理，也没有验证像素级幻觉检测器。更多独立
地理来源、空间分组推断或纹理消融需要另行预设，不能回头修改已消费测试。
论文的完成标准是完整预设比较及可复现报告，包括负结果；不是保证得到正结果。

## 7. Conclusion

在本次冻结外部样本中，K5相对LR改善了R9排序风险，复用同一K5预测的邻域适配
进一步降低平均AURC。优势随指标和尺度变化，额外采样的部署开销仍需与使用情境
一起衡量。固定阈值的覆盖在Crops与Urban间明显不同；排序改进、阈值迁移和有限
样本风险确认是相互关联但需要分别验证的三个问题。邻域方法的排序优势尚未延伸
为其阈值迁移结论，外部描述性发现也不会改写原内部 inconclusive 终局。
